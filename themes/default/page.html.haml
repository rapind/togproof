#intro
  %h1= (@page.intro.blank? ? (@page.title) : (@page.intro))
#content
  #main
    = image_tag(@page.image.url, :alt => (@page.title)) if @page.image?
    = @page.body.markdown.html_safe
  #sidebar
    = render :partial => '/sidebar'
