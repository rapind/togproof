- content_for :title, 'Forgot your password?'
- content_for :sidebar do
  %p You can reset your password if you forgot it or simply would like to change it.
  %p Once you submit your email address we will send you instructions to change your password.

= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
  = f.error_notification
  .inputs= f.input :email, :required => true
  .actions= f.button :submit, 'Send me password reset instructions', :class => 'primary'

= render :partial => 'devise/shared/links'