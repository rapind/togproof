= simple_form_for(@private_gallery, :url => admin_private_gallery_path, :method => :put, :html => { :class => 'form-vertical' }) do |form|
  = render form

%div{ :style => "width:100%; float:left;" }
  %br
  %ul.image_list
    = render :partial => "admin/photos/photo", :collection => @private_gallery.photos