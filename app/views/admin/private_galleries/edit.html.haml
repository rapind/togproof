= simple_form_for(@private_gallery, :url => resource_path, :method => :put, :html => { :class => 'form-vertical' }) do |form|
  = render form
  
%div{ :style => "width:100%; float:left;" }
  %br
  %ul.image_list
    = render :partial => "photo", :collection => @private_gallery.photos