= form_for @gallery_photo do |f|
  -if @gallery_photo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@gallery_photo.errors.count, "error")} prohibited this gallery_photo from being saved:"
      %ul
        - @gallery_photo.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
